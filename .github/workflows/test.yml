name: Test

on:
  push:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name : Build Cmake
      run : ./CMakeGCC.sh
    - name: branche
      run: cd ./bin/make
    - name: make
      run: make
    - name: branche2
      run: cd Release
    - name: execute
      run: ./sobel
